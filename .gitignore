# ===== Node / Next.js build =====
node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo
.env*.local
.vercel/

# ===== Tooling / OS / editor =====
.claude/*
!.claude/skills/
.DS_Store
Thumbs.db
*.log

# ===== Tài liệu nội bộ — KHÔNG đưa lên repo code (mật/scoping) =====
00-boi-canh/
01-nghien-cuu/
02-dac-ta/
03-ke-hoach-ngan-sach/
04-pitch/
STATUS.md
/README.md
memory/
09-so-tay-nguoi-dung/
10-trai-nghiem-nen-tang/
11-khao-sat-yeu-cau/
/AI-Native-PRD-Template.md.docx
/PRD-Prompt-System.md.docx

# ===== Nguồn dữ liệu thô & kho lưu — nặng, không thuộc mã nguồn =====
06-tu-dien-tac-vu/Archive/
06-tu-dien-tac-vu/Archive.zip
gg-io-nhg/

# ===== [Graphify T1 — G1] Đầu ra bản đồ tri thức — KHÔNG BAO GIỜ vào git =====
# graph.json/graph.html dựng từ spec + BRD là DẪN XUẤT của tài liệu mật đang bị loại ở khối
# trên. Commit nó là lách chính sách "tài liệu nội bộ không vào repo code" bằng cửa sau —
# nội dung vẫn ra ngoài, chỉ đổi định dạng. Dòng này phải có TRƯỚC lần chạy đầu tiên.
graphify-out/
.graphify-cache/

# ===== Hồ sơ khách hàng THẬT — mật, không bao giờ vào git =====
# 12-khach-hang/00-tai-san-ban-hang/ (bản trắng + bản mẫu hư cấu) ĐƯỢC theo dõi.
# Mọi thư mục theo năm chứa hồ sơ khách hàng thật thì KHÔNG.
12-khach-hang/[0-9][0-9][0-9][0-9]/
